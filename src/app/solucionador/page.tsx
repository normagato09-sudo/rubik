import { SolverScreen } from "@/features/solver/components/SolverScreen";

export const metadata = {
  title: "Solucionador — RUBIKO",
};

export default function SolucionadorPage() {
  return <SolverScreen />;
}
